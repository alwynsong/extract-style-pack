---
name: extract-style-pack
description: 从用户提供的图片、本地视频或网络公开视频中提取可复用的影视与视觉风格包。适用于“提取某部电影、剧集、广告、MV 或游戏的视觉风格”“分析参考图并生成色卡、风格规则和提示词”“使用视频抽帧建立风格参考”等任务。
---

# 提取视觉风格包

把参考素材整理为结构稳定、证据可追溯、可供后续生成任务直接使用的风格包。

## 开始前

1. 始终先读 [输入与输出契约](references/输入与输出契约.md)。
2. 只在相应阶段继续读取：
   - 输入含网络视频或需要抽帧：读 [网络视频与抽帧](references/网络视频与抽帧.md)。
   - 开始逐图分析、分组和写作：读 [视觉分析与分组](references/视觉分析与分组.md)。
   - 开始生成色卡或验收：读 [色卡与验收](references/色卡与验收.md)。
3. 不要把 `README.md` 当作执行规范；它只面向人类读者。

## 工作原则

- 先保留证据，再形成结论。每条风格规则应能追溯到具体图像或帧。
- 逐图分析在前，跨图分组在后。分组按视觉特征，不按电影“幕”或时间段；时间跨度很远的场景可以属于同一风格组。
- 脚本只负责确定性工作：媒体检测、抽帧、去重、拼图、色卡和结构校验。
- Agent 负责需要视觉判断的工作：来源选择、坏帧复核、逐图标注、分组、归纳、写作和最终视觉检查。
- 不为本任务创建状态机、阶段日志、断点文件或额外编排器；已有产物就是进度依据。
- 不绕过 DRM、登录、付费墙、验证码或访问限制；无法合法取得的素材应请用户提供本地文件。

## 标准流程

### 0. 预检媒体环境（视频任务强制）

输入含网络视频时，先读取 [网络视频与抽帧](references/网络视频与抽帧.md)，并运行：

```bash
python -X utf8 scripts/extract_video_frames.py --check
```

网络视频流程必须能使用 `yt-dlp`、FFmpeg 与 FFprobe；本地视频流程至少必须能使用 FFmpeg 与 FFprobe。缺失时，Agent 应先定位已有安装，再按当前环境授权规则主动发起安装并复检；同一依赖最多安装一次、复检一次。依赖就绪前不要开始下载或抽帧。

### 1. 明确目标与来源

确认作品、版本、媒介、输出目录和用户强调的重点。同名或版本会显著影响结果时先消歧；不影响方向的小缺口可采用保守假设，并写入 `00-说明.md`。

建立风格包目录和 `参考素材/manifest.json`。网络搜索、来源比较与选择由 Agent 完成。每项素材都要记录来源、访问说明、本地路径和处理状态。

### 2. 准备本地媒体

把图片和视频分别放入：

```text
参考素材/原始文件/图片/
参考素材/原始文件/视频/
```

运行：

```bash
python -X utf8 scripts/prepare_media.py "<风格包目录>"
```

检查 `分析/媒体检测结果.json` 与 `分析/去重与筛选记录.json`。自动结果只用于初筛，不替代人工视觉判断。

### 3. 获取网络视频并抽帧（按需）

预检通过后，网络视频使用 `yt-dlp` 等成熟工具下载为本地文件，再调用独立抽帧脚本；不要把站点下载逻辑写入抽帧脚本。

```bash
python -X utf8 scripts/extract_video_frames.py \
  "<本地视频>" \
  --asset-id "VID-0001" \
  --output-dir "<风格包目录>/参考素材/视频抽帧/VID-0001"
```

生成视频审核拼图：

```bash
python -X utf8 scripts/render_style_pack.py "<风格包目录>" --mode review
```

Agent 查看拼图，剔除片头片尾、字幕遮挡、黑场、转场、严重运动模糊和叙事重复帧。只有通过复核的帧进入正式分析。

### 4. 逐图分析与分组

按 [视觉分析与分组](references/视觉分析与分组.md) 完成：

1. 为每张正式图片或帧记录空间、主体比例、构图、光线、色彩、镜头质感、材质、氛围、可迁移特征和画面专属证据。
2. 写入 `分析/单图标签-第一版.json`；禁止直接用组级套话代替逐图观察。
3. 基于已观察到的视觉特征建立分组，将成员复制到 `参考素材/分组/NN-[分组名]/`。
4. 写 `分析/分组总结.md` 与同名分组提示词。一个画面可归入多个有证据的组，但不要为覆盖素材制造大量弱组。

### 5. 生成最终拼图与色卡

先生成最终视觉资产，再写引用颜色值的文档，避免反复回填：

```bash
python -X utf8 scripts/render_style_pack.py \
  "<风格包目录>" \
  --mode final \
  --title "<项目标题>"
```

Agent 必须查看全部素材总览、分组总览和两套色卡总览。脚本色卡是数据起点，不是审美结论；必要时调整 `分析/色卡数据.json` 后重新渲染。已有 JSON 会被保留并用于重渲染；只有分组或素材改变时才使用 `--recompute-palette` 重新取色。

### 6. 写规则与提示词

依据逐图证据、分组总结和最终色卡，一次性完成：

- `00-说明.md`
- `design.md`
- `分析/风格规则-第一版.md`
- `分析/分组提示词/NN-[分组名].md`

文档中的 HEX 必须来自 `分析/色卡数据.json`。提示词描述可迁移规律，不依赖受保护角色名、演员肖像或原作专名才能成立。

### 7. 校验与交付

运行独立校验器：

```bash
python -X utf8 scripts/validate_style_pack.py \
  "<风格包目录>" \
  --visual-review-confirmed
```

校验报告固定写入 `分析/验收报告.json`。按报告修复并重跑，直到没有错误。最后在 `00-说明.md` 记录来源范围、缺失项、人工调整和不确定性。

## 完成条件

只有同时满足以下条件才算完成：

- 来源和本地素材可追溯；
- 每张正式画面都有逐图标签；
- 分组有明确视觉证据，且不是按时间段机械划分；
- 最终拼图和色卡已经人工查看；
- 文档颜色与 `分析/色卡数据.json` 一致；
- 校验器无错误；
- 使用边界和不确定性已写明。

若因来源不可访问、素材不足，或依赖在一次安装与复检后仍不可用，保留已有产物，并说明唯一阻塞项与恢复方式。
